void main() {
  var mensaje = saludar();
  print(mensaje);
}

String saludar() {
  //Se utilizan funciones para reutilizar código, para organizar el código, para abstraer la lógica y para mejorar la legibilidad del código.
  //print('Hola Josué');
  //print('Hola Suyapa');

  return 'Hola Josue';

  //return 0; //El return es opcional, si no se especifica, la función devuelve null por defecto. Este se utiliza para indicar que la función no devuelve ningún valor, o para finalizar la ejecución de la función antes de llegar al final del bloque de código.
}
